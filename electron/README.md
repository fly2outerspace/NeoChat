# NeoChat Electron 桌面应用

这是 NeoChat 的 Electron 桌面应用包装器，提供一键启动功能，统一管理前端和后端进程。

## 📋 目录

- [开发模式](#开发模式)
- [构建应用](#构建应用)
- [架构设计](#架构设计)
- [故障排除](#故障排除)

---

## 🚀 开发模式

### 快速开始

首次安装依赖：

```bash
# 安装前端依赖
cd frontend/web-chat
npm install  # 或 pnpm install

# 安装 Electron 依赖
cd ../../electron
npm install
```

日常开发（一个终端即可）：

```bash
cd electron
npm run dev
```

**说明**：
- Electron 会自动检测并启动前端 dev server（端口 3000）
- Electron 会自动启动 Python 后端服务器（端口 8000）
- 如果前端 dev server 已在运行，Electron 会复用现有实例
- 应用退出时，所有进程会被优雅关闭（FastAPI 清理代码会执行）

### 开发工作流

#### 标准开发流程

1. **启动开发环境**：
   ```bash
   cd electron
   npm run dev
   ```

2. **修改代码**：
   - 前端代码：修改 `frontend/web-chat` 中的文件，Next.js 会自动热重载
   - 后端代码：修改 `app/` 中的文件，需要重启 Electron 才能生效

3. **查看日志**：
   - 前端日志：浏览器控制台
   - 后端日志：Electron 控制台（实时显示）

4. **关闭应用**：
   - 正常关闭窗口或按 Ctrl+C
   - 所有进程会自动优雅关闭

#### 调试技巧

- **查看后端日志**：Electron 控制台会实时显示后端输出
- **查看前端日志**：浏览器开发者工具
- **单独启动前端**：在 `frontend/web-chat` 目录运行 `npm run dev`，Electron 会复用
- **单独启动后端**：直接运行 `python run_api.py`（仅启动后端）

### 端口配置

- 后端 API 默认运行在 `http://localhost:8000`
- 前端开发服务器运行在 `http://localhost:3000`
- 确保这些端口未被占用

---

## 📦 构建应用

### 前置要求

1. **Node.js** (v18+)
2. **Python** (3.8+)
3. **PyInstaller** (用于打包 Python 后端)
   ```bash
   pip install pyinstaller
   ```

### 一键构建（推荐）

使用 PowerShell 构建脚本，一键完成所有步骤：

```powershell
cd electron
.\build.ps1
```

构建脚本会自动完成：
1. ✅ 检查前置条件（Node.js、Python、PyInstaller）
2. ✅ 构建前端静态文件（`npm run build && npm run export`）
3. ✅ 打包 Python 后端（使用 PyInstaller）
4. ✅ 处理 Meilisearch（如果提供）
5. ✅ 更新配置文件（使用相对路径）
6. ✅ 构建 Electron 应用（生成安装包）

### 手动构建步骤

如果不想使用构建脚本，可以手动执行以下步骤：

#### 步骤 1: 准备应用图标（必需）

将应用图标放在 `electron/build/icon.ico`：
- 格式：`.ico`（Windows）或 `.png`（其他平台）
- 尺寸：至少 256x256 像素

#### 步骤 2: 准备 Meilisearch（可选）

将 `meilisearch-windows-amd64.exe` 放在：

```
electron/
└── meilisearch/
    └── meilisearch-windows-amd64.exe
```

**如果没有 Meilisearch**：应用仍可正常构建和运行，但搜索功能不可用。

#### 步骤 3: 构建前端

```bash
cd frontend/web-chat
npm install
npm run build
npm run export
```

验证输出：
- 检查 `frontend/web-chat/out/` 目录是否存在
- 检查 `frontend/web-chat/out/index.html` 是否存在

#### 步骤 4: 打包 Python 后端

```bash
# 在项目根目录
pyinstaller --onefile --name server --add-data "config;config" --add-data "app;app" --hidden-import=uvicorn --hidden-import=fastapi --hidden-import=app.api.main --hidden-import=app.storage.meilisearch_service --hidden-import=app.storage.database --hidden-import=app.storage.settings_database run_api.py

# 复制到 electron/backend
copy dist\server.exe electron\backend\server.exe
```

#### 步骤 5: 更新版本号（可选）

编辑 `electron/package.json`：
```json
{
  "version": "1.0.0"  // 更新为你的版本号
}
```

#### 步骤 6: 构建 Electron 应用

```bash
cd electron
npm run dist
```

或者只构建 Windows 版本：
```bash
npm run dist:win
```

### 构建输出

构建完成后，在 `electron/dist/` 目录下会生成：

```
electron/dist/
├── NeoChat Setup x.x.x.exe    # NSIS 安装包（用于分发）
└── win-unpacked/               # 未打包版本（用于测试）
    ├── NeoChat.exe             # 主程序
    └── resources/              # 应用资源
        ├── app.asar            # 打包的应用代码（前端）
        └── backend/             # 后端资源
            ├── server.exe       # Python 后端可执行文件
            ├── config/          # 配置文件
            │   └── config.toml
            └── meilisearch/     # Meilisearch（如果提供）
                └── meilisearch.exe
```

### 测试构建结果

#### 测试安装包

1. 在干净的 Windows 系统（或虚拟机）上测试
2. 运行安装包 `NeoChat Setup x.x.x.exe`
3. 安装到默认位置或自定义位置
4. 启动应用，检查：
   - 应用是否能正常启动
   - 前端界面是否正常显示
   - 后端 API 是否正常响应
   - 所有功能是否正常工作

#### 测试未打包版本（更快）

```bash
cd electron/dist/win-unpacked
.\NeoChat.exe
```

这样可以更快地测试，无需安装。

### 发布准备

#### 压缩安装包

```powershell
# 在 electron/dist/ 目录下
# 使用 PowerShell
Compress-Archive -Path "NeoChat Setup x.x.x.exe" -DestinationPath "NeoChat_vx.x.x_Setup.zip"
```

#### 压缩便携版（可选）

```powershell
# 压缩未打包版本
Compress-Archive -Path "win-unpacked\*" -DestinationPath "NeoChat_vx.x.x_Portable.zip" -Force
```

### 构建配置说明

#### 后端路径配置

- **开发环境**: Electron 会尝试运行项目根目录的 `run_api.py`
- **生产环境**: Electron 会在 `resources/backend/` 目录查找后端可执行文件

#### 前端路径配置

- **开发环境**: Electron 自动检测并启动前端 dev server（端口 3000）
- **生产环境**: 加载本地静态文件（`resources/frontend/index.html`）

#### Meilisearch 配置

构建脚本会自动：
1. 从 `electron/meilisearch/meilisearch-windows-amd64.exe` 复制
2. 复制到 `electron/backend/meilisearch/meilisearch.exe`
3. 更新配置文件，使用相对路径 `meilisearch/meilisearch.exe`
4. 将 Meilisearch 打包到最终的应用中

配置文件会自动更新为使用相对路径：

```toml
[meilisearch]
executable_path = "meilisearch/meilisearch.exe"  # 相对路径
db_path = "meili_data"  # 数据目录（相对于 server.exe）
```

---

## 🏗️ 架构设计

### 进程管理

Electron 使用统一的 `ProcessManager` 类管理所有子进程：

- **前端 dev server**（开发模式）：由 Electron 直接管理
- **后端 API 服务器**：由 Electron 启动 `run_api.py`（仅启动后端，不管理前端）

### 优雅关闭机制

- 使用 `tree-kill` 库实现跨平台进程树管理
- 先发送 `SIGTERM` 信号，允许 FastAPI 执行清理代码（如停止 Meilisearch）
- 5 秒超时后强制终止（`SIGKILL`）
- 确保所有子进程都被正确终止

### 开发规范

1. **前端开发**：
   - Electron 会自动管理前端 dev server
   - 如需手动启动前端（例如调试），在 `frontend/web-chat` 目录运行 `npm run dev`
   - Electron 会检测到已运行的前端并复用

2. **后端开发**：
   - `run_api.py` 仅负责启动 FastAPI 后端服务器
   - 不再管理前端进程（避免冲突）
   - 后端日志会实时显示在 Electron 控制台

3. **进程生命周期**：
   - 启动：前端和后端并行启动
   - 运行：所有进程由 ProcessManager 统一跟踪
   - 关闭：应用退出时统一优雅关闭所有进程

### 目录结构

```
electron/
├── main.js              # Electron 主进程（进程管理和窗口）
├── preload.js           # 预加载脚本（安全桥接）
├── package.json         # Electron 配置和依赖
├── build.ps1            # 一键构建脚本
├── update_config.py     # 配置文件更新脚本
├── build/               # 构建资源
│   └── icon.ico         # 应用图标
├── backend/             # 后端资源（构建时生成）
│   ├── server.exe       # 后端可执行文件
│   ├── config/          # 配置文件
│   └── meilisearch/     # Meilisearch
└── meilisearch/         # Meilisearch 源文件（构建前放置）
    └── meilisearch-windows-amd64.exe
```

---

## 🔧 故障排除

### 开发模式问题

#### 后端无法启动

- 确保已安装 Python 3.8+
- 检查项目根目录是否存在 `run_api.py`
- 查看 Electron 控制台的后端日志

#### 前端无法启动

- 检查端口 3000 是否被占用
- 检查 `frontend/web-chat` 目录是否存在
- 检查 `node_modules` 是否已安装
- 如果 `.next/dev/lock` 文件存在，删除它后重试

#### 前端无法连接后端

- 检查后端是否在 `http://localhost:8000` 运行
- 检查前端配置中的 API URL
- 查看浏览器控制台的网络错误

#### 进程无法正常关闭

- 检查是否有其他程序占用端口
- 查看 Electron 控制台的关闭日志
- 如果进程卡死，可以手动终止进程

#### 端口被占用

**Windows**:
```bash
# 查看占用端口的进程
netstat -ano | findstr :8000
netstat -ano | findstr :3000

# 终止进程（替换 PID 为实际进程 ID）
taskkill /F /PID <PID>
```

**macOS/Linux**:
```bash
# 查看占用端口的进程
lsof -i :8000
lsof -i :3000

# 终止进程
kill -9 <PID>
```

### 构建问题

#### 构建失败：找不到图标

**解决方案**：
1. 创建 `electron/build/icon.ico` 文件（至少 256x256 像素）
2. 或修改 `package.json` 移除图标配置（会使用默认图标）

#### 构建失败：前端未构建

**解决方案**：
```bash
cd frontend/web-chat
npm run build
npm run export
```

#### 构建失败：后端打包失败

**可能原因**：
1. PyInstaller 打包不完整（缺少依赖）
2. 配置文件路径错误
3. 数据库路径问题

**解决方案**：
1. 检查 PyInstaller 的 `--hidden-import` 参数
2. 检查 `main.js` 中的后端路径配置
3. 查看 Electron 控制台的错误日志

#### 构建失败：文件被占用

**解决方案**：
1. 关闭所有 NeoChat/Electron 窗口
2. 关闭文件资源管理器中的 `electron/dist` 目录
3. 重新运行构建脚本

#### 应用启动失败：后端未找到

**可能原因**：
1. 后端可执行文件路径错误
2. 配置文件路径错误

**解决方案**：
1. 检查 `electron/dist/win-unpacked/resources/backend/server.exe` 是否存在
2. 查看 Electron 控制台的路径解析日志
3. 检查 `main.js` 中的路径配置

#### 应用启动失败：前端灰屏

**可能原因**：
1. 前端静态文件路径错误
2. 前端静态文件未正确打包

**解决方案**：
1. 检查 `electron/dist/win-unpacked/resources/frontend/index.html` 是否存在
2. 检查 `package.json` 中的 `files` 和 `extraResources` 配置
3. 查看 Electron 控制台的前端加载日志

---

## 📝 功能特性

- ✅ 统一进程管理：前端和后端由 Electron 统一管理
- ✅ 自动检测：智能检测前端 dev server 是否已运行
- ✅ 优雅关闭：确保 FastAPI 清理代码正常执行
- ✅ 跨平台支持：Windows、macOS、Linux
- ✅ 开发/生产模式：自动切换开发和生产环境
- ✅ 一键构建：使用 PowerShell 脚本一键完成所有构建步骤

---

## 📚 相关文档

- 查看项目根目录的 README 了解整体架构
- 查看 `main.js` 了解进程管理实现细节
- 查看 `build.ps1` 了解构建流程细节
